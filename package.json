{
  "name": "apitestproject",
  "version": "1.0.0",
  "description": "Multi-agent test automation pipeline — auto-generated tests",
  "scripts": {
    "test:cypress": "cypress run --reporter junit",
    "test:playwright": "playwright test",
    "test:all": "npm run test:cypress && npm run test:playwright"
  },
  "devDependencies": {
    "cypress": "^13.0.0",
    "@playwright/test": "^1.44.0",
    "mocha-junit-reporter": "^2.2.1",
    "typescript": "^5.4.0"
  }
}
