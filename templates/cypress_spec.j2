// Auto-generated Cypress spec for {{ ticket_id }}
// Generated by multi-agent test automation pipeline — do not edit manually.
{% if language == "typescript" %}
import { {{ page_name }}Page } from '../pages/{{ page_name }}Page';

describe('{{ ticket_id }} — Auto-generated tests', () => {
  const page = new {{ page_name }}Page();

  beforeEach(() => {
    page.visit();
  });
  {% for test_case in test_cases %}

  it('{{ test_case.test_id }}: {{ test_case.title }}', () => {
    // @jira {{ test_case.ticket_id }}
    // Type: {{ test_case.type }}
    // Given: {{ test_case.given }}
    // When:  {{ test_case.when }}
    // Then:  {{ test_case.then }}

    // TODO: implement test body
    page.perform{{ test_case.test_id | replace("-", "") | replace("_", "") }}();
    // TODO: add assertions for — {{ test_case.then }}
  });
  {% endfor %}
});
{% else %}
const { {{ page_name }}Page } = require('../pages/{{ page_name }}Page');

describe('{{ ticket_id }} — Auto-generated tests', () => {
  const page = new {{ page_name }}Page();

  beforeEach(() => {
    page.visit();
  });
  {% for test_case in test_cases %}

  it('{{ test_case.test_id }}: {{ test_case.title }}', () => {
    // @jira {{ test_case.ticket_id }}
    // Type: {{ test_case.type }}
    // Given: {{ test_case.given }}
    // When:  {{ test_case.when }}
    // Then:  {{ test_case.then }}

    page.perform{{ test_case.test_id | replace("-", "") | replace("_", "") }}();
    // TODO: add assertions for — {{ test_case.then }}
  });
  {% endfor %}
});
{% endif %}
